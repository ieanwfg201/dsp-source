package com.kritter.api_caller.core.controller;

import java.util.Map;

/**
 * This interface is marker interface to act as stitcher between
 * inhouse api and external api.
 * Two kind of mappings will be there:
 * 1. Inhouse api to external webservice api.
 * 2. Inhouse api to special signature (db connector).
 */

public interface InhouseExternalApiStitcher {




}
