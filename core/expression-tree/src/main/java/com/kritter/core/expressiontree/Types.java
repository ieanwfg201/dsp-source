package com.kritter.core.expressiontree;

/**
 * Types which are supported in the boolean expression parser and evualuator.
 */
public enum Types {
    STRING,
    INTEGER,
    LONG,
    FLOAT,
    DOUBLE,
    BOOLEAN
}
